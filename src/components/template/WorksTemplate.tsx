import React from 'react';
import Head from 'next/head';

interface Type {
  title?: string;
  content?: string;
}

const WorksTemplate = () => {
  return <div>works 템플릿</div>;
};

export default WorksTemplate;
